/**
 * @param {number} n
 * @return {[number]}
 */
const grayCode = function(n) {
    if (n === 0) {
        return [0];
    }

};

console.log(grayCode(2)); // [0,1,3,2] / [0,2,3,1]
console.log(grayCode(0)); // [0]
